{
  "name": "experiments-test",
  "version": "1.0.0",
  "scripts": {
    "test": "start-server-and-test start http://localhost:8080/experiments.html test:run",
    "start": "http-server -p 8080",
    "test:run": "node test_experiments.js"
  },
  "dependencies": {
    "d3": "^5.16.0",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "start-server-and-test": "^2.1.3"
  }
}
